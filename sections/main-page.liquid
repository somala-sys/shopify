<section class="section">
  <div class="container">
    <div class="section-head">
      <div>
        <h1 class="section-title">{{ page.title }}</h1>
        <p class="section-sub">{{ settings.page_subtitle }}</p>
      </div>
    </div>

    <div class="card" style="padding:18px;">
      <div class="footer-text">{{ page.content }}</div>
    </div>
  </div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
